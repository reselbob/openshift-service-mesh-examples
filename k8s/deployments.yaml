apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendations-food
  namespace: service-mesh-demo
  labels:
    app: recommendations
    version: food
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recommedations
      version: food
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "true"
      labels:
        app: recommedations
        version: food
    spec:
      containers:
      - name: recommendations-food
        image: quay.io/reselbob/service-mesh-recommemdations:v1.0
        imagePullPolicy: IfNotPresent
        env:
        - name: RECOMMENDATION_CATEGORY
          value: "FOOD"
        ports:
        - containerPort: 8080
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendations-music
  namespace: service-mesh-demo
  labels:
    app: recommendations
    version: music
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recommedations
      version: music
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "true"
      labels:
        app: recommedations
        version: music
    spec:
      containers:
      - name: recommendations-music
        image: quay.io/reselbob/service-mesh-recommemdations:v1.0
        imagePullPolicy: IfNotPresent
        env:
        - name: RECOMMENDATION_CATEGORY
          value: "MUSIC"
        ports:
        - containerPort: 8080
