apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: service-mesh-demo-destination-rule
  namespace: service-mesh-demo
spec:
  host: orders
  trafficPolicy:
    loadBalancer:
      simple: RANDOM
  subsets:
  - name: recommendation
    labels:
      app: recommendation
  - name: recommendation-food
    labels:
      app: recommendation-food
